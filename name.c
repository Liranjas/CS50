#include <stdio.h>
int main (void)
{
    int first, last;
    printf("type first name: ");
    scanf("%d",&first);
    printf("type second name: ");
    scanf("%d",&last);

    printf("hello, %d %d \n ", first, last);
}